# ~/.ha-tools venv — the ONLY place python deps can go on Ubuntu (PEP 668).
# Pinned from the working laptop 2026-09-16 (em-Latitude-6430U, Python 3.12.3).
# Top-level tools:
homeassistant-cli==0.9.6
yamllint==1.38.0
# Pulled in by the above but pinned explicitly because repo tooling imports them
# directly (reference_dev_ha_tooling.md):
ruamel.yaml==0.17.40
netdisco==3.0.0
aiohttp==3.14.3
dateparser==0.7.6
